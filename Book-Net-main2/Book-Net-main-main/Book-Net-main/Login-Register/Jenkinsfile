pipeline {
    agent any

    tools {
        nodejs "NodeJS" // Make sure NodeJS is configured in Jenkins
    }

    environment {
        CLIENT_DIR = 'Login-register/client'
        SERVER_DIR = 'Login-register/server'
        TESTS_DIR = 'Login-register/tests'
    }

    stages {
        stage('Install Server Dependencies') {
            steps {
                dir("${SERVER_DIR}") {
                    bat 'npm install'
                }
            }
        }

        stage('Install Client Dependencies') {
            steps {
                dir("${CLIENT_DIR}") {
                    bat 'npm install'
                }
            }
        }

        stage('Build Client') {
            steps {
                dir("${CLIENT_DIR}") {
                    bat 'npm run build'
                }
            }
        }

        stage('Run Server Unit Tests') {
            steps {
                dir("${SERVER_DIR}") {
                    bat 'npm test'
                }
            }
            post {
                always {
                    junit "${SERVER_DIR}/reports/**/*.xml" // adjust if your tests generate XML reports
                }
            }
        }

        stage('Run Client Unit Tests') {
            steps {
                dir("${CLIENT_DIR}") {
                    bat 'npm test'
                }
            }
        }

        stage('Run Automation Tests') {
            steps {
                dir("${TESTS_DIR}") {
                   
                    bat 'npm run test' // or 'npm run test:e2e' depending on your script
                }
            }
            post {
                always {
                    publishHTML(target: [
                        reportDir: "${TESTS_DIR}/reports",
                        reportFiles: 'index.html',
                        reportName: 'Automation Test Report'
                    ])
                }
            }
        }
    }

    post {
        success {
            echo 'CI/CD pipeline completed successfully!'
        }
        failure {
            echo 'CI/CD pipeline failed.'
        }
    }
}

